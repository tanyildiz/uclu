<div class="col-lg-9 col-md-9 col-sm-6 col-xs-12">

	<%= form_for @category do |c| %>
		
		<%= c.label :name %><br />
		<%= c.text_field :name, class:'form-control', :autofocus => true%>

		<%= c.submit 'OK', class: 'btn btn-primary' %>

	<% end %>

</div>